# Bot de trading v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refondre le bot F8 (v1 → v2) : profil auto par titre, sessions de marché, boucle intraday, stop suiveur, sortie partielle, plafonds, frais/slippage, apprentissage par famille, statistiques, UI refondue — sans casser les bots v1 en production.

**Architecture:** Fonctions **pures** (`botMarketOf`, `botOpenMarkets`, `botProfile`, `botComputeLearning`, `botStats`) testées par un `botSelfTest()` appelable en console. Moteur `runBot()` réécrit autour du profil + sessions + risque. UI F8 refondue en sections repliables. Migration douce depuis l'état v1.

**Tech Stack:** HTML/CSS/JS vanilla, fichier unique `terminal-tout-en-un.html`. Aucune modif serveur ni dépendance.

## Global Constraints

- **Simulateur 100 % virtuel.** Aucun ordre réel, aucun courtier. Disclaimer permanent affiché.
- **Best-effort** : une panne (réseau, données manquantes) ne casse jamais l'app.
- Pas de build, fichier unique, français, `esc()` pour tout contenu injecté.
- Convention mono-fichier : les tests JS sont dans une fonction `botSelfTest()` appelée depuis la console (le projet n'a pas de harnais JS).
- Migration v1 → v2 **obligatoire** (des bots tournent déjà) : aucun perte de position ni d'historique.
- Réglages de stratégie (stop/cible/taille) **non modifiables** par l'utilisateur : c'est l'objectif de cette version. Seuls restent réglables les paramètres de §G du spec.
- Les stats **recalculées depuis `bot.history`** à chaque `runBot()`, jamais accumulées.
- `esc()`, `fnum()`, `fpct()`, `pctClass()`, `computeGlobalScore()`, `cache`, `marketCache`, `analyzeTicker`, `isStale`, `toast` : existants — les réutiliser.
- Lancement local : `py server.py`, ouvrir `http://localhost:8750/terminal-tout-en-un.html`. Test serveur : `py -m unittest discover tests`.

---

### Task 1: Sessions de marché (fonctions pures) + `botSelfTest()`

**Files:**
- Modify: `terminal-tout-en-un.html` — remplacer la section BOT existante (~ligne 1400–1560) par une section « BOT V2 ». Cette task ajoute uniquement les **fonctions pures** en tête de section ; la v1 reste temporairement en place en-dessous jusqu'à la Task 4.

**Interfaces:**
- Consumes: aucune (fonctions pures, seule dépendance : `Intl.DateTimeFormat`).
- Produces:
  - `botMarketOf(symbol) → "EU" | "US" | "ASIA"`
  - `botOpenMarkets(now = new Date()) → Set<"EU"|"US"|"ASIA">`
  - `botNextOpen(market, now = new Date()) → number` (ms avant prochaine ouverture)
  - `botSelfTest() → { pass, fail, total, report }` — scaffolding vide qui logue le rapport dans la console.

- [ ] **Step 1: Insérer les fonctions et le scaffold de tests**

Juste avant `const BOT_DEFAULT_CONFIG` (~ligne 1409), insérer :

```js
/* ============================= BOT V2 — SESSIONS ============================= */

// EU / US / ASIA d'après le suffixe Yahoo (ex. ASML.AS → EU, 7203.T → ASIA, BRK-B → US).
function botMarketOf(symbol) {
  if (typeof symbol !== "string" || !symbol) return "US";
  const s = symbol.toUpperCase();
  const dot = s.lastIndexOf(".");
  if (dot < 0) return "US"; // pas de suffixe → US (couvre BRK-B, MSFT, etc.)
  const suf = s.slice(dot); // inclut le point
  const EU   = new Set([".PA",".AS",".BR",".LS",".MC",".MI",".DE",".F",".SW",".L",".VI",".HE",".ST",".OL",".CO",".IR"]);
  const ASIA = new Set([".T",".KS",".KQ",".HK",".SS",".SZ",".AX",".NS",".BO",".SI",".TW"]);
  if (EU.has(suf)) return "EU";
  if (ASIA.has(suf)) return "ASIA";
  return "US";
}

// Séances (heure locale via Intl → gère automatiquement DST). Lun–ven.
const BOT_SESSIONS = {
  EU:   { tz: "Europe/Paris",     openMin:  9*60,      closeMin: 17*60 + 30 },
  US:   { tz: "America/New_York", openMin:  9*60 + 30, closeMin: 16*60      },
  ASIA: { tz: "Asia/Tokyo",       openMin:  9*60,      closeMin: 15*60      },
};

// { weekday: "Mon"|... , hour, minute } pour un fuseau donné à `now`.
function botLocalParts(tz, now) {
  const parts = new Intl.DateTimeFormat("en-GB", {
    timeZone: tz, hour12: false, weekday: "short", hour: "2-digit", minute: "2-digit",
  }).formatToParts(now);
  const get = (t) => parts.find(p => p.type === t)?.value ?? "";
  return { weekday: get("weekday"), hour: Number(get("hour")), minute: Number(get("minute")) };
}

function botIsWeekday(weekday) {
  return weekday === "Mon" || weekday === "Tue" || weekday === "Wed" || weekday === "Thu" || weekday === "Fri";
}

function botOpenMarkets(now = new Date()) {
  const open = new Set();
  for (const m of ["EU","US","ASIA"]) {
    const s = BOT_SESSIONS[m];
    const { weekday, hour, minute } = botLocalParts(s.tz, now);
    if (!botIsWeekday(weekday)) continue;
    const mins = hour * 60 + minute;
    if (mins >= s.openMin && mins < s.closeMin) open.add(m);
  }
  return open;
}

// ms avant la prochaine ouverture du marché `m`. 0 si déjà ouvert. Plafonné à 7 jours.
function botNextOpen(market, now = new Date()) {
  if (botOpenMarkets(now).has(market)) return 0;
  const s = BOT_SESSIONS[market];
  // recherche itérative par pas de 15 min, plafonnée à 7 jours (672 pas)
  const stepMs = 15 * 60 * 1000;
  for (let i = 1; i <= 7 * 24 * 4; i++) {
    const t = new Date(now.getTime() + i * stepMs);
    if (botOpenMarkets(t).has(market)) return i * stepMs;
  }
  return 7 * 24 * 3600 * 1000;
}

/* ============================= BOT V2 — SELF TEST ============================= */

const BOT_TEST_CASES = []; // rempli par les tasks suivantes via botTest(name, fn)

function botTest(name, fn) { BOT_TEST_CASES.push({ name, fn }); }
function botAssert(cond, msg) { if (!cond) throw new Error(msg || "assertion failed"); }
function botAssertEq(a, b, msg) {
  const eq = (Number.isFinite(a) && Number.isFinite(b)) ? Math.abs(a - b) < 1e-9 : a === b;
  if (!eq) throw new Error(`${msg || "not equal"} — got ${JSON.stringify(a)}, expected ${JSON.stringify(b)}`);
}
function botAssertClose(a, b, tol, msg) {
  if (!Number.isFinite(a) || !Number.isFinite(b) || Math.abs(a - b) > tol) {
    throw new Error(`${msg || "not close"} — got ${a}, expected ~${b} (tol ${tol})`);
  }
}

function botSelfTest() {
  let pass = 0, fail = 0;
  const report = [];
  for (const { name, fn } of BOT_TEST_CASES) {
    try { fn(); pass++; report.push({ name, ok: true }); }
    catch (e) { fail++; report.push({ name, ok: false, err: String(e && e.message || e) }); }
  }
  const total = BOT_TEST_CASES.length;
  console.log(`[botSelfTest] ${pass}/${total} passed, ${fail} failed`);
  for (const r of report) console.log(r.ok ? `  ✓ ${r.name}` : `  ✗ ${r.name} — ${r.err}`);
  return { pass, fail, total, report };
}

/* ============================= BOT V2 — TESTS: SESSIONS ============================= */

botTest("botMarketOf: suffixes", () => {
  botAssertEq(botMarketOf("ASML.AS"), "EU");
  botAssertEq(botMarketOf("MC.PA"),   "EU");
  botAssertEq(botMarketOf("7203.T"),  "ASIA");
  botAssertEq(botMarketOf("0700.HK"), "ASIA");
  botAssertEq(botMarketOf("AAPL"),    "US");
  botAssertEq(botMarketOf("BRK-B"),   "US");        // tiret ≠ point
  botAssertEq(botMarketOf("ZZZ.XX"),  "US");        // suffixe inconnu → défaut US
  botAssertEq(botMarketOf(""),        "US");
});

botTest("botOpenMarkets: hiver — matin EU seul", () => {
  // 2027-01-11 (lundi) 10:00 Paris = 09:00 UTC
  const t = new Date(Date.UTC(2027, 0, 11, 9, 0));
  const o = botOpenMarkets(t);
  botAssert( o.has("EU"),  "EU devrait être ouvert");
  botAssert(!o.has("US"),  "US devrait être fermé");
  botAssert(!o.has("ASIA"),"ASIA devrait être fermé");
});

botTest("botOpenMarkets: hiver — chevauchement EU + US", () => {
  // 2027-01-11 (lundi) 16:00 Paris = 15:00 UTC → NY 10:00
  const t = new Date(Date.UTC(2027, 0, 11, 15, 0));
  const o = botOpenMarkets(t);
  botAssert(o.has("EU") && o.has("US"), "EU et US doivent être ouverts simultanément");
});

botTest("botOpenMarkets: été — chevauchement EU + US (DST)", () => {
  // 2027-07-05 (lundi) 16:00 Paris = 14:00 UTC → NY 10:00 EDT
  const t = new Date(Date.UTC(2027, 6, 5, 14, 0));
  const o = botOpenMarkets(t);
  botAssert(o.has("EU") && o.has("US"), "EU et US doivent être ouverts en été aussi");
});

botTest("botOpenMarkets: soirée US seule", () => {
  // 2027-01-11 (lundi) 21:00 Paris = 20:00 UTC → NY 15:00
  const t = new Date(Date.UTC(2027, 0, 11, 20, 0));
  const o = botOpenMarkets(t);
  botAssert(!o.has("EU") && o.has("US") && !o.has("ASIA"));
});

botTest("botOpenMarkets: nuit — tout fermé", () => {
  // 2027-01-11 (lundi) 03:00 UTC → EU 04:00 fermé, NY 22:00 dim fermé, TYO 12:00 → ouvert !
  // On teste plutôt 06:00 UTC : EU 07:00 fermé, NY 01:00 fermé, TYO 15:00 fermé (close 15:00 exclus).
  const t = new Date(Date.UTC(2027, 0, 11, 6, 0));
  const o = botOpenMarkets(t);
  botAssertEq(o.size, 0, "aucun marché ouvert attendu");
});

botTest("botOpenMarkets: week-end fermé partout", () => {
  const t = new Date(Date.UTC(2027, 0, 9, 12, 0)); // samedi
  botAssertEq(botOpenMarkets(t).size, 0);
});

botTest("botNextOpen: 0 quand ouvert", () => {
  const t = new Date(Date.UTC(2027, 0, 11, 9, 0));   // Paris 10:00 lundi → EU ouvert
  botAssertEq(botNextOpen("EU", t), 0);
});

botTest("botNextOpen: strictement positif quand fermé", () => {
  const t = new Date(Date.UTC(2027, 0, 9, 12, 0));   // samedi
  botAssert(botNextOpen("US", t) > 0);
});
```

- [ ] **Step 2: Vérifier les tests dans la console**

Lancer `py server.py`, ouvrir `http://localhost:8750/terminal-tout-en-un.html`, DevTools console → `botSelfTest()`.
Attendu : `8/8 passed, 0 failed`.

- [ ] **Step 3: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): détection de marché EU/US/ASIA et sessions (fonctions pures + botSelfTest)"
```

---

### Task 2: Profil par titre `botProfile()`

**Files:**
- Modify: `terminal-tout-en-un.html` — ajouter à la fin de la section « BOT V2 — SESSIONS » (avant la v1 encore présente).

**Interfaces:**
- Consumes: `clamp` (à réutiliser depuis la v1 si déjà présent ; sinon le déclarer ici — voir Step 1), `botMarketOf` (Task 1).
- Produces:
  - `botProfile(entry, cfg, learn, portfolioValue, cash) → { market, volBucket, family, stopPct, trailPct, rr, targetPct, amount, horizonDays, why }`
  - `BOT_V2_DEFAULT_CONFIG` (constante).

- [ ] **Step 1: Ajouter la config par défaut, `clamp` si absent, et `botProfile`**

Vérifier que `clamp` existe déjà (défini v1 ligne ~1422). Si oui, ne pas le redéclarer. Sinon, l'ajouter en tête de la section BOT V2 :
```js
function clamp(x, a, b) { return Math.max(a, Math.min(b, x)); }
```

Puis ajouter :

```js
/* ============================= BOT V2 — CONFIG PAR DÉFAUT ============================= */

const BOT_V2_DEFAULT_CONFIG = {
  capital: 10000,
  qualityMin: 60, exitScore: 40,
  stopVolFactor: 0.40, stopMin: 5, stopMax: 20,
  rrMin: 1.5, rrMax: 3,
  riskPerTradePct: 1.0, maxPositionPct: 15, maxMarketPct: 60,
  feePct: 0.10, slipPct: 0.05,
  autoLoop: true, loopMinutes: 15,
  learnEnabled: true, learnMinTrades: 10,
};

/* ============================= BOT V2 — PROFIL PAR TITRE ============================= */

function botVolBucket(vol) {
  if (vol < 20) return "faible";
  if (vol < 40) return "moyenne";
  return "forte";
}

function botHorizonDays(bucket) {
  return bucket === "faible" ? 60 : bucket === "moyenne" ? 45 : 30;
}

// Profil complet d'un candidat. `entry` = objet marketCache (avec `ind`, `score`, `ticker`).
// `learn` = état d'apprentissage (peut être vide → neutre). `portfolioValue`/`cash` : plafonds.
// `marketExposure` (optionnel) : € déjà exposé sur ce marché, pour le plafond marché.
function botProfile(entry, cfg, learn, portfolioValue, cash, marketExposure = 0) {
  const ticker = entry.ticker;
  const ind = entry.ind || {};
  const market = botMarketOf(ticker);
  const vol = (ind.vol != null && isFinite(ind.vol)) ? ind.vol : 30;
  const volBucket = botVolBucket(vol);
  const family = `${market}:${volBucket}`;
  const fam = (learn && learn[family]) || { stopMult: 1, rrMult: 1, qualityAdj: 0 };
  const score = (entry.score != null) ? computeGlobalScore(entry) : cfg.qualityMin;

  // Stop initial
  const stopPct = clamp(cfg.stopVolFactor * fam.stopMult * vol, cfg.stopMin, cfg.stopMax);

  // Stop suiveur : plus serré que le stop initial (protège le gain)
  const trailPct = 0.8 * stopPct;

  // Ratio gain/risque
  let rr = cfg.rrMin + Math.max(0, (score - cfg.qualityMin)) / Math.max(1, 100 - cfg.qualityMin) * (cfg.rrMax - cfg.rrMin);
  if (ind.price > 0 && ind.sma50 > 0 && ind.sma200 > 0 && ind.price > ind.sma50 && ind.sma50 > ind.sma200) rr += 0.3;
  if (ind.rangePos != null && ind.rangePos > 90) rr -= 0.3;
  rr = clamp(rr * fam.rrMult, 1.2, 4);
  const targetPct = stopPct * rr;

  // Taille par le risque
  const risquePct = cfg.riskPerTradePct / 100;
  const stopFrac = stopPct / 100;
  const amountRisque = (portfolioValue * risquePct) / stopFrac;      // capital risqué constant
  let amount = clamp(amountRisque * (score / 75), 0.5 * amountRisque, 1.5 * amountRisque);

  // Plafonds
  const capPos = portfolioValue * (cfg.maxPositionPct / 100);
  const marginMarket = Math.max(0, portfolioValue * (cfg.maxMarketPct / 100) - marketExposure);
  amount = Math.min(amount, capPos, marginMarket, Math.max(0, cash));

  const horizonDays = botHorizonDays(volBucket);

  const why = `score ${Math.round(score)}, vol ${vol.toFixed(0)} % (${family}). Stop ${stopPct.toFixed(1)} %, cible ${targetPct.toFixed(1)} % (RR ${rr.toFixed(1)}). Mise ${Math.round(amount)} € = ${cfg.riskPerTradePct} % de risque.`;

  return { market, volBucket, family, stopPct, trailPct, rr, targetPct, amount, horizonDays, why };
}
```

- [ ] **Step 2: Ajouter les tests `botProfile`**

À la fin de la section (après les tests sessions) :

```js
/* ============================= BOT V2 — TESTS: PROFIL ============================= */

// Fixture minimale d'un candidat.
function botMkEntry(ticker, over = {}) {
  return {
    ticker,
    score: over.score ?? 75,
    ind: { price: 100, vol: 30, sma50: 95, sma200: 90, rangePos: 50, ...(over.ind || {}) },
  };
}

botTest("botProfile: stop borné 5–20 %", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  const low  = botProfile(botMkEntry("A", { ind: { vol: 5 }  }), cfg, {}, 10000, 10000);
  const high = botProfile(botMkEntry("B", { ind: { vol: 80 } }), cfg, {}, 10000, 10000);
  botAssertEq(low.stopPct, 5);
  botAssertEq(high.stopPct, 20);
});

botTest("botProfile: RR croît avec le score", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  const lo = botProfile(botMkEntry("A", { score: 60, ind: { rangePos: 50 } }), cfg, {}, 10000, 10000);
  const hi = botProfile(botMkEntry("B", { score: 95, ind: { rangePos: 50 } }), cfg, {}, 10000, 10000);
  botAssert(hi.rr > lo.rr, "RR devrait croître avec le score");
});

botTest("botProfile: +0,3 tendance haussière", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  const trend = botProfile(botMkEntry("A", { ind: { price: 100, sma50: 95, sma200: 90 } }), cfg, {}, 10000, 10000);
  const flat  = botProfile(botMkEntry("B", { ind: { price: 100, sma50: 100, sma200: 100 } }), cfg, {}, 10000, 10000);
  botAssert(trend.rr > flat.rr, "tendance devrait ajouter au RR");
});

botTest("botProfile: −0,3 haut de range", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  const top    = botProfile(botMkEntry("A", { ind: { rangePos: 95 } }), cfg, {}, 10000, 10000);
  const middle = botProfile(botMkEntry("B", { ind: { rangePos: 50 } }), cfg, {}, 10000, 10000);
  botAssert(top.rr < middle.rr, "haut de range devrait pénaliser le RR");
});

botTest("botProfile: risque € constant quelle que soit la vol", () => {
  const cfg = { ...BOT_V2_DEFAULT_CONFIG, maxPositionPct: 100, maxMarketPct: 100 };
  const calm  = botProfile(botMkEntry("A", { ind: { vol: 15 } }), cfg, {}, 10000, 10000);
  const wild  = botProfile(botMkEntry("B", { ind: { vol: 45 } }), cfg, {}, 10000, 10000);
  // amount * stopPct/100 = € risqué → constant
  const riskCalm = calm.amount * calm.stopPct / 100;
  const riskWild = wild.amount * wild.stopPct / 100;
  botAssertClose(riskCalm, riskWild, riskCalm * 0.02, "risque € doit être identique");
});

botTest("botProfile: plafond position", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG; // maxPositionPct 15 %
  const p = botProfile(botMkEntry("A", { score: 95, ind: { vol: 10 } }), cfg, {}, 100000, 100000);
  botAssert(p.amount <= 100000 * 0.15 + 1e-6, "montant plafonné à 15 % du portefeuille");
});

botTest("botProfile: plafond marché", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG; // maxMarketPct 60 %
  // 55 % déjà exposé sur US → il reste au plus 5 % = 5000 €
  const p = botProfile(botMkEntry("AAPL"), cfg, {}, 100000, 100000, 55000);
  botAssert(p.amount <= 5000 + 1e-6, "montant plafonné par la marge marché restante");
});

botTest("botProfile: horizon par bucket", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  botAssertEq(botProfile(botMkEntry("A", { ind: { vol: 10 } }), cfg, {}, 1e5, 1e5).horizonDays, 60);
  botAssertEq(botProfile(botMkEntry("B", { ind: { vol: 30 } }), cfg, {}, 1e5, 1e5).horizonDays, 45);
  botAssertEq(botProfile(botMkEntry("C", { ind: { vol: 60 } }), cfg, {}, 1e5, 1e5).horizonDays, 30);
});

botTest("botProfile: apprentissage appliqué", () => {
  const cfg = BOT_V2_DEFAULT_CONFIG;
  const neutral = botProfile(botMkEntry("A", { ind: { vol: 30 } }), cfg, {}, 1e5, 1e5);
  const learn = { "US:moyenne": { stopMult: 1.3, rrMult: 0.8, qualityAdj: 0 } };
  const learned = botProfile(botMkEntry("A", { ind: { vol: 30 } }), cfg, learn, 1e5, 1e5);
  botAssert(learned.stopPct > neutral.stopPct, "stopMult devrait élargir le stop");
  botAssert(learned.rr < neutral.rr, "rrMult devrait réduire le RR");
});
```

- [ ] **Step 2 bis: Vérifier**

Recharger, console → `botSelfTest()`. Attendu : `17/17 passed`.

- [ ] **Step 3: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): botProfile (stop/trail/RR/taille par le risque/horizon) + tests"
```

---

### Task 3: Migration d'état v1 → v2 + réécriture de `runBot()`

**Files:**
- Modify: `terminal-tout-en-un.html` — remplacer **intégralement** la v1 (bloc `BOT_DEFAULT_CONFIG`, `let bot = …`, `botStopPct`, `botRR`, `botTargetPct`, `botPositionAmount`, `botFindExit`, `botSellManual`, `runBot`, `botStart`, `botReset`) par la version v2. Conserver `botPortfolioValue`, `botCurrentPrice` (utilisés ailleurs) — ils continuent à fonctionner tels quels.

**Interfaces:**
- Consumes: `BOT_V2_DEFAULT_CONFIG`, `botProfile`, `botMarketOf`, `botOpenMarkets`, `clamp`, `lsGet`, `lsSet`, `computeGlobalScore`, `cache`, `marketCache`, `analyzeTicker`, `isStale`.
- Produces:
  - `bot` (état v2 avec `positions[]`, `history[]`, `log[]`, `equity[]`, `learn{}`, `config`)
  - `saveBot()`, `botStart()`, `botReset()`, `botSellManual(ticker)`
  - `runBot()` — sessions + botProfile + rejeu des clôtures pour les sorties (v1 conservé, sans encore les frais/stop suiveur/partiel/plafonds — ajoutés tasks 5-8).
  - `botMigrateV1(raw) → bot` (pure).

- [ ] **Step 1: Écrire le remplacement complet**

Remplacer les lignes 1409–1559 (approx.) par :

```js
/* ============================= BOT V2 — ÉTAT + MIGRATION ============================= */

// Retourne un état v2 valide à partir d'un état brut (v1, v2, null, cassé).
function botMigrateV1(raw) {
  const cfg = { ...BOT_V2_DEFAULT_CONFIG, ...(raw && raw.config || {}) };
  // v1 avait ticketPct : on l'écarte silencieusement (remplacé par riskPerTradePct)
  delete cfg.ticketPct;
  const b = {
    started:   !!(raw && raw.started),
    startDate: (raw && raw.startDate) || null,
    cash:      (raw && isFinite(raw.cash)) ? raw.cash : cfg.capital,
    positions: Array.isArray(raw && raw.positions) ? raw.positions : [],
    history:   Array.isArray(raw && raw.history)   ? raw.history   : [],
    log:       Array.isArray(raw && raw.log)       ? raw.log       : [],
    equity:    Array.isArray(raw && raw.equity)    ? raw.equity    : [],
    learn:     (raw && raw.learn && typeof raw.learn === "object") ? raw.learn : {},
    config:    cfg,
  };
  // Compléter les positions v1 avec les champs v2 manquants.
  b.positions = b.positions.map(p => {
    const market = p.market || botMarketOf(p.ticker);
    const volBucket = p.volBucket || "moyenne";
    const family = p.family || `${market}:${volBucket}`;
    const stopPct = isFinite(p.stopPct) ? p.stopPct : 10;
    const trailPct = isFinite(p.trailPct) ? p.trailPct : 0.8 * stopPct;
    const targetPct = isFinite(p.targetPct) ? p.targetPct : stopPct * 2;
    const stopLevel = isFinite(p.stopLevel) ? p.stopLevel : p.entryPrice * (1 - stopPct / 100);
    const highest = isFinite(p.highest) ? p.highest : p.entryPrice;
    return {
      ticker: p.ticker, market, family,
      entryDate: p.entryDate, entryPrice: p.entryPrice, qty: p.qty,
      stopPct, trailPct, targetPct,
      stopLevel, highest,
      horizonDays: isFinite(p.horizonDays) ? p.horizonDays : botHorizonDays(volBucket),
      scaledOut: !!p.scaledOut,
      entryScore: isFinite(p.entryScore) ? p.entryScore : null,
    };
  });
  // Compléter l'historique avec `family` si absente (déduite du ticker).
  b.history = b.history.map(h => h.family ? h : { ...h, family: `${botMarketOf(h.ticker)}:moyenne` });
  return b;
}

let bot = botMigrateV1(lsGet(LS.bot, null));
function saveBot() { lsSet(LS.bot, bot); }

/* ============================= BOT V2 — MOTEUR ============================= */

// Cherche le 1er jour après achat où le stop ou la cible est franchi (sur clôtures).
// Utilise stopLevel courant (qui peut être remonté par le suiveur — pris en charge en Task 5).
function botFindExit(pos, e) {
  if (!e || !e.hist || !Array.isArray(e.hist.closes) || !Array.isArray(e.hist.dates)) return null;
  const stopLevel = pos.stopLevel;
  const targetLevel = pos.entryPrice * (1 + pos.targetPct / 100);
  const { dates, closes } = e.hist;
  for (let i = dates.length - 1; i >= 0; i--) {
    if (dates[i] <= pos.entryDate) continue;
    const c = closes[i];
    if (c == null || !isFinite(c)) continue;
    if (c <= stopLevel)   return { price: c, date: dates[i], reason: "stop-loss" };
    if (c >= targetLevel) return { price: c, date: dates[i], reason: "prise de bénéfice" };
  }
  return null;
}

function botSellManual(ticker) {
  const idx = bot.positions.findIndex(p => p.ticker === ticker);
  if (idx < 0) return;
  const pos = bot.positions[idx];
  const e = cache[ticker] || marketCache[ticker];
  const price = (e && e.ind && e.ind.price > 0) ? e.ind.price : pos.entryPrice;
  bot.cash += pos.qty * price;
  bot.history.unshift({ ticker, family: pos.family, entryDate: pos.entryDate, entryPrice: pos.entryPrice,
    exitDate: new Date().toISOString().slice(0, 10), exitPrice: price, qty: pos.qty,
    pnl: (price - pos.entryPrice) * pos.qty, pnlPct: (price / pos.entryPrice - 1) * 100, reason: "manuelle" });
  bot.positions.splice(idx, 1);
  saveBot();
  if (typeof renderBot === "function") renderBot();
}

let botRunning = false; // garde de réentrance

async function runBot() {
  if (!bot.started || botRunning) return;
  botRunning = true;
  try {
    const cfg = bot.config;
    const today = new Date().toISOString().slice(0, 10);
    const openMarkets = botOpenMarkets();

    // a) rafraîchir les positions (best-effort)
    for (const pos of bot.positions) {
      const e = cache[pos.ticker] || marketCache[pos.ticker];
      if (!e || isStale(e)) {
        try { await analyzeTicker(pos.ticker, null, { silent: true, skipRender: true, store: cache[pos.ticker] ? cache : marketCache }); }
        catch (_) { /* on évalue avec ce qu'on a */ }
      }
    }

    // b) sorties
    const stillOpen = [];
    for (const pos of bot.positions) {
      const e = cache[pos.ticker] || marketCache[pos.ticker];
      let exit = e ? botFindExit(pos, e) : null;

      // Sortie en direct uniquement si le marché du titre est ouvert (spec A.3).
      if (!exit && e && e.ind && openMarkets.has(pos.market) && isFinite(e.ind.price) && e.ind.price > 0) {
        if (e.ind.price <= pos.stopLevel)                                    exit = { price: e.ind.price, date: today, reason: "stop-loss" };
        else if (e.ind.price >= pos.entryPrice * (1 + pos.targetPct / 100))  exit = { price: e.ind.price, date: today, reason: "prise de bénéfice" };
      }

      // Réévaluation : score trop bas → sortie même hors séance (cours = dernier connu)
      if (!exit && e && e.ind) {
        const g = e.score != null ? computeGlobalScore(e) : null;
        if ((g != null && g <= cfg.exitScore) || e.signal === "Vente") {
          exit = { price: e.ind.price, date: today, reason: "réévaluation" };
        }
      }

      if (exit && isFinite(exit.price) && exit.price > 0) {
        bot.cash += pos.qty * exit.price;
        bot.history.unshift({ ticker: pos.ticker, family: pos.family, entryDate: pos.entryDate, entryPrice: pos.entryPrice,
          exitDate: exit.date, exitPrice: exit.price, qty: pos.qty,
          pnl: (exit.price - pos.entryPrice) * pos.qty, pnlPct: (exit.price / pos.entryPrice - 1) * 100, reason: exit.reason });
      } else {
        stillOpen.push(pos);
      }
    }
    bot.positions = stillOpen;

    // c) entrées
    const held = new Set(bot.positions.map(p => p.ticker));
    // exposition par marché (€) — pour le plafond
    const exposureByMarket = {};
    for (const pos of bot.positions) {
      const e = cache[pos.ticker] || marketCache[pos.ticker];
      const price = (e && e.ind && e.ind.price > 0) ? e.ind.price : pos.entryPrice;
      exposureByMarket[pos.market] = (exposureByMarket[pos.market] || 0) + pos.qty * price;
    }
    const portfolioValue = botPortfolioValue();

    const candidates = Object.keys(marketCache)
      .map(t => ({ t, e: marketCache[t] }))
      .filter(x => x.e && x.e.ind && x.e.ind.price > 0 && !held.has(x.t) && x.e.score != null && computeGlobalScore(x.e) >= cfg.qualityMin && openMarkets.has(botMarketOf(x.t)))
      .sort((a, b) => computeGlobalScore(b.e) - computeGlobalScore(a.e));

    for (const cand of candidates) {
      const market = botMarketOf(cand.t);
      const marketExp = exposureByMarket[market] || 0;
      const prof = botProfile({ ticker: cand.t, ...cand.e }, cfg, bot.learn, portfolioValue, bot.cash, marketExp);
      if (prof.amount < 1) continue;
      if (bot.cash < prof.amount) continue;
      const price = cand.e.ind.price;
      const qty = prof.amount / price;
      bot.cash -= prof.amount;
      bot.positions.push({
        ticker: cand.t, market: prof.market, family: prof.family,
        entryDate: today, entryPrice: price, qty,
        stopPct: prof.stopPct, trailPct: prof.trailPct, targetPct: prof.targetPct,
        stopLevel: price * (1 - prof.stopPct / 100),
        highest: price,
        horizonDays: prof.horizonDays,
        scaledOut: false,
        entryScore: cand.e.score != null ? computeGlobalScore(cand.e) : null,
      });
      exposureByMarket[market] = marketExp + prof.amount;
      held.add(cand.t);
    }

    if (bot.history.length > 200) bot.history = bot.history.slice(0, 200);
    saveBot();
    if (typeof renderBot === "function") renderBot();
  } finally {
    botRunning = false;
  }
}

function botStart() {
  bot.started = true; bot.startDate = new Date().toISOString();
  bot.cash = bot.config.capital; bot.positions = []; bot.history = [];
  bot.log = []; bot.equity = []; bot.learn = {};
  saveBot();
  if (typeof renderBot === "function") renderBot();
  runBot();
}

function botReset() {
  if (!window.confirm("Réinitialiser le bot ? Positions, historique, journal et apprentissage seront effacés.")) return;
  bot.started = false; bot.startDate = null;
  bot.cash = bot.config.capital; bot.positions = []; bot.history = [];
  bot.log = []; bot.equity = []; bot.learn = {};
  saveBot();
  if (typeof renderBot === "function") renderBot();
}
```

- [ ] **Step 2: Adapter `BOT_CONFIG_FIELDS` pour la v2**

Chercher `BOT_CONFIG_FIELDS` dans le fichier (utilisé par `renderBot`, cfgEl). Remplacer par :

```js
const BOT_CONFIG_FIELDS = [
  ["capital",         "Capital initial (€)",          "100"],
  ["riskPerTradePct", "Risque par trade (%)",         "0.1"],
  ["maxPositionPct",  "Plafond par position (%)",     "1"],
  ["maxMarketPct",    "Plafond par marché (%)",       "5"],
  ["qualityMin",      "Score min. d'achat",           "1"],
  ["exitScore",       "Score de sortie",              "1"],
  ["feePct",          "Frais (%)",                    "0.01"],
  ["slipPct",         "Slippage (%)",                 "0.01"],
  ["loopMinutes",     "Boucle (minutes)",             "1"],
];
```

Les champs stop/cible/taille (ticketPct, stopVolFactor, stopMin, stopMax, rrMin, rrMax) **ne sont plus exposés** dans l'UI — spec §G.

- [ ] **Step 3: Ajouter les tests migration**

Après les tests botProfile :

```js
/* ============================= BOT V2 — TESTS: MIGRATION ============================= */

botTest("botMigrateV1: état null → v2 vide valide", () => {
  const b = botMigrateV1(null);
  botAssertEq(b.started, false);
  botAssert(Array.isArray(b.positions) && b.positions.length === 0);
  botAssert(Array.isArray(b.log));
  botAssert(Array.isArray(b.equity));
  botAssert(b.learn && typeof b.learn === "object");
  botAssertEq(b.config.riskPerTradePct, 1);
});

botTest("botMigrateV1: v1 avec positions → complétées", () => {
  const v1 = {
    started: true, startDate: "2026-07-01", cash: 5000,
    positions: [{ ticker: "ASML.AS", entryDate: "2026-07-02", entryPrice: 800, qty: 5, stopPct: 12, targetPct: 24, entryScore: 78 }],
    history: [{ ticker: "MC.PA", entryDate: "2026-06-01", exitDate: "2026-06-10", entryPrice: 700, exitPrice: 720, qty: 10, pnl: 200, pnlPct: 2.86, reason: "prise de bénéfice" }],
    config: { capital: 10000, ticketPct: 0.1, qualityMin: 60 },
  };
  const b = botMigrateV1(v1);
  botAssertEq(b.positions.length, 1);
  const p = b.positions[0];
  botAssertEq(p.market, "EU");
  botAssertEq(p.family, "EU:moyenne");
  botAssertEq(p.trailPct, 0.8 * 12);
  botAssertEq(p.stopLevel, 800 * (1 - 12 / 100));
  botAssertEq(p.highest, 800);
  botAssertEq(p.scaledOut, false);
  botAssertEq(b.history[0].family, "EU:moyenne");
  botAssert(!("ticketPct" in b.config), "ticketPct doit être supprimé");
  botAssertEq(b.config.riskPerTradePct, 1, "riskPerTradePct par défaut");
});
```

- [ ] **Step 4: Vérifier**

Recharger. Vérifier que l'onglet F8 s'affiche sans erreur (bot v1 existant migré si présent, sinon vide). Console : `botSelfTest()` → `19/19 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): moteur, migration v1→v2 et sessions filtrant achats/sorties"
```

---

### Task 4: Boucle intraday

**Files:**
- Modify: `terminal-tout-en-un.html` — remplacer le `if (bot.started) { setTimeout(() => runBot()…) }` unique (~ligne 3796) par un scheduler qui se relance ; ajouter listener `visibilitychange`.

**Interfaces:**
- Consumes: `runBot`, `botOpenMarkets`, `botNextOpen`, `bot.config.autoLoop`, `bot.config.loopMinutes`.
- Produces: `botScheduleNext()` (idempotent).

- [ ] **Step 1: Remplacer l'appel unique par un scheduler**

Chercher `if (bot.started) { setTimeout(() => runBot().catch(() => {}), 1500); }` (~ligne 3796). Remplacer par :

```js
let botTimer = null;

function botScheduleNext() {
  if (botTimer) { clearTimeout(botTimer); botTimer = null; }
  if (!bot.started || !bot.config.autoLoop) return;
  if (typeof document !== "undefined" && document.visibilityState !== "visible") return;
  const open = botOpenMarkets();
  let delayMs;
  if (open.size > 0) {
    delayMs = Math.max(1, bot.config.loopMinutes) * 60 * 1000;
  } else {
    // hors séance : prochaine ouverture (marché le plus proche), plafonné à 30 min
    const next = Math.min(botNextOpen("EU"), botNextOpen("US"), botNextOpen("ASIA"));
    delayMs = Math.min(next || 30 * 60 * 1000, 30 * 60 * 1000);
  }
  botTimer = setTimeout(async () => {
    try { await runBot(); } catch (_) {}
    botScheduleNext();
  }, delayMs);
}

if (bot.started) { setTimeout(() => runBot().catch(() => {}).finally(botScheduleNext), 1500); }
if (typeof document !== "undefined") {
  document.addEventListener("visibilitychange", () => {
    if (document.visibilityState === "visible") botScheduleNext();
    else if (botTimer) { clearTimeout(botTimer); botTimer = null; }
  });
}
```

- [ ] **Step 2: Relancer le scheduler après start/reset**

Dans `botStart()` remplacer `runBot();` par `runBot().finally(botScheduleNext);`.
Dans `botReset()` ajouter `if (botTimer) { clearTimeout(botTimer); botTimer = null; }` juste avant `saveBot();`.

- [ ] **Step 3: Vérifier manuellement**

Recharger, démarrer le bot, ouvrir DevTools → `botTimer` doit être défini. Passer l'onglet en arrière-plan → `botTimer` devient `null`. Revenir → il se replanifie.

- [ ] **Step 4: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): boucle intraday (autoLoop, visibilité, hors-séance reprogrammé)"
```

---

### Task 5: Stop suiveur (`highest` + `stopLevel` monotone)

**Files:**
- Modify: `terminal-tout-en-un.html` — modifier `runBot()` (bloc « b) sorties ») et `botFindExit`.

**Interfaces:**
- Consumes: état existant sur les positions (`highest`, `stopLevel`, `trailPct`).
- Produces: mise à jour de `pos.highest` et `pos.stopLevel` à chaque évaluation. Raison de sortie `"stop suiveur"`.

- [ ] **Step 1: Mise à jour du plus-haut et du stop, au rejeu et en direct**

Dans `runBot()`, remplacer le bloc « b) sorties » — juste après avoir rafraîchi et **avant** `botFindExit` — insérer une mise à jour du `highest` et un recalcul du `stopLevel` monotone, en s'appuyant sur les clôtures postérieures à l'entrée et sur le cours courant si le marché est ouvert.

Remplacer la boucle des sorties par :

```js
// b) sorties
const stillOpen = [];
for (const pos of bot.positions) {
  const e = cache[pos.ticker] || marketCache[pos.ticker];

  // 1) Rejeu chronologique des clôtures depuis l'entrée : met à jour highest + stopLevel
  //    puis détecte la 1re sortie sur stop courant / cible.
  let exit = null;
  if (e && e.hist && Array.isArray(e.hist.closes) && Array.isArray(e.hist.dates)) {
    const { dates, closes } = e.hist;
    // dates du plus récent au plus ancien : on parcourt à l'envers pour l'ordre chronologique
    for (let i = dates.length - 1; i >= 0; i--) {
      if (dates[i] <= pos.entryDate) continue;
      const c = closes[i];
      if (!isFinite(c)) continue;
      // suiveur : n'active qu'après 1× le risque de gain latent
      if (c > pos.highest) pos.highest = c;
      const gainLatent = (pos.highest - pos.entryPrice) / pos.entryPrice;
      const risque = pos.stopPct / 100;
      if (gainLatent >= risque) {
        const trail = pos.highest * (1 - pos.trailPct / 100);
        if (trail > pos.stopLevel) pos.stopLevel = trail;
      }
      if (c <= pos.stopLevel) {
        exit = { price: c, date: dates[i], reason: pos.stopLevel > pos.entryPrice * (1 - pos.stopPct / 100) + 1e-9 ? "stop suiveur" : "stop-loss" };
        break;
      }
      const targetLevel = pos.entryPrice * (1 + pos.targetPct / 100);
      if (c >= targetLevel) { exit = { price: c, date: dates[i], reason: "prise de bénéfice" }; break; }
    }
  }

  // 2) En direct si marché ouvert
  if (!exit && e && e.ind && openMarkets.has(pos.market) && isFinite(e.ind.price) && e.ind.price > 0) {
    const live = e.ind.price;
    if (live > pos.highest) pos.highest = live;
    const gainLatent = (pos.highest - pos.entryPrice) / pos.entryPrice;
    if (gainLatent >= pos.stopPct / 100) {
      const trail = pos.highest * (1 - pos.trailPct / 100);
      if (trail > pos.stopLevel) pos.stopLevel = trail;
    }
    if (live <= pos.stopLevel) {
      exit = { price: live, date: today, reason: pos.stopLevel > pos.entryPrice * (1 - pos.stopPct / 100) + 1e-9 ? "stop suiveur" : "stop-loss" };
    } else if (live >= pos.entryPrice * (1 + pos.targetPct / 100)) {
      exit = { price: live, date: today, reason: "prise de bénéfice" };
    }
  }

  // 3) Réévaluation par le score (indépendante des sessions)
  if (!exit && e && e.ind) {
    const g = e.score != null ? computeGlobalScore(e) : null;
    if ((g != null && g <= cfg.exitScore) || e.signal === "Vente") {
      exit = { price: e.ind.price, date: today, reason: "réévaluation" };
    }
  }

  if (exit && isFinite(exit.price) && exit.price > 0) {
    bot.cash += pos.qty * exit.price;
    bot.history.unshift({ ticker: pos.ticker, family: pos.family, entryDate: pos.entryDate, entryPrice: pos.entryPrice,
      exitDate: exit.date, exitPrice: exit.price, qty: pos.qty,
      pnl: (exit.price - pos.entryPrice) * pos.qty, pnlPct: (exit.price / pos.entryPrice - 1) * 100, reason: exit.reason });
  } else {
    stillOpen.push(pos);
  }
}
bot.positions = stillOpen;
```

Supprimer l'ancien `botFindExit` (plus utilisé).

- [ ] **Step 2: Ajouter les tests stop suiveur (fonction pure isolée)**

Extraire la logique de mise à jour en fonction pure testable. En haut de la section BOT V2 MOTEUR :

```js
// Applique un tick (nouveau prix) à une position : met à jour highest/stopLevel et retourne
// { updated: pos, exit: {price, reason} | null }. reason ∈ "stop-loss" | "stop suiveur" | "prise de bénéfice" | null.
function botApplyTick(pos, price, dateStr) {
  const p = { ...pos };
  if (!isFinite(price) || price <= 0) return { updated: p, exit: null };
  if (price > p.highest) p.highest = price;
  const initialStop = pos.entryPrice * (1 - pos.stopPct / 100);
  const gainLatent = (p.highest - pos.entryPrice) / pos.entryPrice;
  if (gainLatent >= pos.stopPct / 100) {
    const trail = p.highest * (1 - pos.trailPct / 100);
    if (trail > p.stopLevel) p.stopLevel = trail;
  }
  if (price <= p.stopLevel) {
    return { updated: p, exit: { price, date: dateStr, reason: p.stopLevel > initialStop + 1e-9 ? "stop suiveur" : "stop-loss" } };
  }
  const targetLevel = pos.entryPrice * (1 + pos.targetPct / 100);
  if (price >= targetLevel) {
    return { updated: p, exit: { price, date: dateStr, reason: "prise de bénéfice" } };
  }
  return { updated: p, exit: null };
}
```

Et remplacer dans `runBot` les deux blocs de calcul par `botApplyTick`. Ensuite ajouter les tests :

```js
botTest("botApplyTick: stop suiveur monotone", () => {
  const pos = { entryPrice: 100, stopPct: 10, trailPct: 8, targetPct: 100, highest: 100, stopLevel: 90 };
  // Série montante 100 → 120 → 130
  let s = botApplyTick(pos,  120, "d1"); botAssertEq(s.exit, null);
  s = botApplyTick(s.updated, 130, "d2");
  botAssert(s.updated.stopLevel > 90, "stopLevel doit avoir monté");
  const highStop = s.updated.stopLevel;
  // Puis rechute 130 → 125 → 120 : le stopLevel ne bouge pas (monotone)
  s = botApplyTick(s.updated, 125, "d3"); botAssertEq(s.updated.stopLevel, highStop);
  s = botApplyTick(s.updated, 120, "d4"); botAssertEq(s.updated.stopLevel, highStop);
});

botTest("botApplyTick: déclenchement au bon prix", () => {
  const pos = { entryPrice: 100, stopPct: 10, trailPct: 8, targetPct: 100, highest: 100, stopLevel: 90 };
  // 100 → 130 (trigger le suiveur à 130 × 0.92 = 119.6) → 118 doit sortir
  let s = botApplyTick(pos, 130, "d1");
  const trail = 130 * 0.92;
  botAssertClose(s.updated.stopLevel, trail, 1e-6);
  s = botApplyTick(s.updated, 118, "d2");
  botAssert(s.exit && s.exit.reason === "stop suiveur", "sortie stop suiveur attendue");
});

botTest("botApplyTick: cible avant que le suiveur ne se déclenche", () => {
  const pos = { entryPrice: 100, stopPct: 10, trailPct: 8, targetPct: 20, highest: 100, stopLevel: 90 };
  const s = botApplyTick(pos, 125, "d1");
  botAssert(s.exit && s.exit.reason === "prise de bénéfice");
});
```

- [ ] **Step 3: Vérifier**

Console : `botSelfTest()` → `22/22 passed`.

- [ ] **Step 4: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): stop suiveur monotone (botApplyTick) + tests"
```

---

### Task 6: Sortie partielle (50 % à la cible)

**Files:**
- Modify: `terminal-tout-en-un.html` — dans `botApplyTick`, remplacer la sortie « prise de bénéfice » par un signal « partiel » quand `scaledOut = false` ; dans `runBot`, traiter le partiel.

**Interfaces:**
- Consumes: `botApplyTick`.
- Produces: raison de sortie `"prise partielle"` dans `bot.history`. La position reste ouverte avec `scaledOut: true` et `qty` divisée par 2.

- [ ] **Step 1: Adapter `botApplyTick` pour signaler un partiel**

Remplacer le bloc « cible atteinte » de `botApplyTick` par :

```js
  if (price >= targetLevel) {
    if (!pos.scaledOut) {
      return { updated: p, exit: { price, date: dateStr, reason: "prise partielle", partial: true } };
    }
    // Une position déjà scaledOut ne re-sort pas sur cible : elle continue sous suiveur uniquement.
    return { updated: p, exit: null };
  }
```

- [ ] **Step 2: Traiter le partiel dans `runBot`**

Dans le bloc de traitement d'`exit` de `runBot`, remplacer par :

```js
      if (exit && isFinite(exit.price) && exit.price > 0) {
        if (exit.partial) {
          // Vendre 50 % de la quantité, garder l'autre moitié sous suiveur
          const soldQty = pos.qty / 2;
          bot.cash += soldQty * exit.price;
          bot.history.unshift({ ticker: pos.ticker, family: pos.family, entryDate: pos.entryDate, entryPrice: pos.entryPrice,
            exitDate: exit.date, exitPrice: exit.price, qty: soldQty,
            pnl: (exit.price - pos.entryPrice) * soldQty, pnlPct: (exit.price / pos.entryPrice - 1) * 100, reason: exit.reason });
          pos.qty = pos.qty - soldQty;
          pos.scaledOut = true;
          stillOpen.push(pos);
        } else {
          bot.cash += pos.qty * exit.price;
          bot.history.unshift({ ticker: pos.ticker, family: pos.family, entryDate: pos.entryDate, entryPrice: pos.entryPrice,
            exitDate: exit.date, exitPrice: exit.price, qty: pos.qty,
            pnl: (exit.price - pos.entryPrice) * pos.qty, pnlPct: (exit.price / pos.entryPrice - 1) * 100, reason: exit.reason });
        }
      } else {
        stillOpen.push(pos);
      }
```

- [ ] **Step 3: Ajouter les tests partiel**

```js
botTest("botApplyTick: partiel une seule fois", () => {
  const pos = { entryPrice: 100, stopPct: 10, trailPct: 8, targetPct: 20, highest: 100, stopLevel: 90, scaledOut: false };
  const s1 = botApplyTick(pos, 125, "d1");
  botAssert(s1.exit && s1.exit.reason === "prise partielle" && s1.exit.partial === true);
  // Une fois scaledOut, plus de sortie sur cible
  const pos2 = { ...pos, scaledOut: true };
  const s2 = botApplyTick(pos2, 130, "d2");
  botAssert(!s2.exit || s2.exit.reason !== "prise partielle", "pas de second partiel");
});
```

- [ ] **Step 4: Vérifier**

Console : `botSelfTest()` → `23/23 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): sortie partielle (50 % à la cible, scaledOut) + tests"
```

---

### Task 7: Frais et slippage

**Files:**
- Modify: `terminal-tout-en-un.html` — dans `runBot`, appliquer `feePct` et `slipPct` à l'entrée **et** à chaque sortie (partielle ou totale, manuelle et automatique).

**Interfaces:**
- Consumes: `bot.config.feePct`, `bot.config.slipPct`.
- Produces: cash, pnl et pnlPct nets de frais/slippage.

- [ ] **Step 1: Extraire helpers purs**

Ajouter en haut de section MOTEUR :

```js
// Prix payé = prix affiché majoré du slippage ; cash retiré inclut la commission.
function botApplyEntryCost(price, amount, cfg) {
  const filled = price * (1 + cfg.slipPct / 100);
  const cash   = amount * (1 + cfg.feePct / 100);
  const qty    = amount / filled;
  return { filled, cash, qty };
}
// Prix reçu = prix affiché minoré du slippage ; cash crédité déduit la commission.
function botApplyExitProceeds(price, qty, cfg) {
  const filled  = price * (1 - cfg.slipPct / 100);
  const gross   = filled * qty;
  const cash    = gross * (1 - cfg.feePct / 100);
  return { filled, cash };
}
```

- [ ] **Step 2: Utiliser ces helpers dans `runBot`**

Dans le bloc « entrées », remplacer :
```js
      bot.cash -= prof.amount;
      bot.positions.push({ …, entryPrice: price, qty: prof.amount / price, … });
```
par :
```js
      const { filled: entryFilled, cash: cashOut, qty } = botApplyEntryCost(price, prof.amount, cfg);
      if (cashOut > bot.cash) continue;
      bot.cash -= cashOut;
      bot.positions.push({
        …
        entryPrice: entryFilled, qty,
        stopLevel: entryFilled * (1 - prof.stopPct / 100),
        highest: entryFilled,
        …
      });
```

Dans le bloc « sorties » (les 2 branches — partielle et totale) :
- calculer `const { cash } = botApplyExitProceeds(exit.price, sellQty, cfg);`
- créditer `bot.cash += cash;`
- pnl : `(cash / sellQty - pos.entryPrice) * sellQty` (net) et `pnlPct = (cash / sellQty / pos.entryPrice - 1) * 100`.

Faire le même changement dans `botSellManual` (vente au cours courant, frais/slippage appliqués).

- [ ] **Step 3: Ajouter les tests**

```js
botTest("frais/slippage: aller-retour à prix constant est perdant", () => {
  const cfg = { ...BOT_V2_DEFAULT_CONFIG, feePct: 0.1, slipPct: 0.05 };
  const price = 100, amount = 1000;
  const entry = botApplyEntryCost(price, amount, cfg);
  const exit  = botApplyExitProceeds(price, entry.qty, cfg);
  const pnl   = exit.cash - amount;
  botAssert(pnl < 0, "aller-retour à prix constant doit être perdant");
  // Coût attendu ~ (2 × slip + 2 × fee) % ≈ 0.30 % ≈ 3 €
  botAssertClose(-pnl, 3, 0.5, "coût total ≈ 3 € sur 1000 €");
});
```

- [ ] **Step 4: Vérifier**

`botSelfTest()` → `24/24 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): frais et slippage à l'entrée et à la sortie + tests"
```

---

### Task 8: Journal de décisions (`bot.log`)

**Files:**
- Modify: `terminal-tout-en-un.html` — ajouter `botLog(kind, ticker, msg)`, l'appeler dans `runBot` aux moments clés, et éviter le spam en dédupliquant les refus par cause + par run.

**Interfaces:**
- Consumes: `bot.log`, `saveBot`.
- Produces: `botLog(kind, ticker, msg)` — insère en tête, plafonne à 150 entrées.

- [ ] **Step 1: Ajouter `botLog`**

Après `saveBot()` :

```js
function botLog(kind, ticker, msg) {
  bot.log.unshift({ ts: new Date().toISOString(), kind, ticker, msg });
  if (bot.log.length > 150) bot.log = bot.log.slice(0, 150);
}
```

- [ ] **Step 2: Instrumenter `runBot`**

- Après chaque `bot.history.unshift(…)` d'une sortie automatique, `botLog(exit.reason === "prise partielle" ? "partiel" : "vente", pos.ticker, «...»);`
- Après chaque achat, `botLog("achat", cand.t, prof.why);`
- Refus : maintenir un `Set<string>` local `loggedRefusals` par run pour ne pas dupliquer la même cause.
  Ex : marché fermé (par marché), plafond position, plafond marché (par marché), quality insuffisant (une fois si aucun candidat).
- Hors séance globale : `botLog("info", null, "Aucun marché ouvert — rattrapage seul");`.

Exemple d'intégration dans le filtre candidats :

```js
const loggedRefusals = new Set();
function refuse(cause) { if (loggedRefusals.has(cause)) return; loggedRefusals.add(cause); botLog("ignoré", null, cause); }

for (const cand of candidates) {
  const market = botMarketOf(cand.t);
  if (!openMarkets.has(market)) { refuse(`marché ${market} fermé`); continue; }
  const marketExp = exposureByMarket[market] || 0;
  const prof = botProfile({ ticker: cand.t, ...cand.e }, cfg, bot.learn, portfolioValue, bot.cash, marketExp);
  if (prof.amount < 1) { refuse(`plafond ${market} atteint ou taille < 1 €`); continue; }
  …
}
```

Adapter le message d'achat à celui du spec §E :
`botLog("achat", cand.t, \`score ${…}, volatilité ${…} % (famille ${prof.family}). Stop ${…} %, cible ${…} % (RR ${prof.rr.toFixed(1)}). Mise ${…} € = ${cfg.riskPerTradePct} % de risque.\`);`

- [ ] **Step 3: Ajouter un test**

```js
botTest("botLog: plafonne à 150 entrées", () => {
  const save = bot.log; bot.log = [];
  for (let i = 0; i < 200; i++) botLog("info", null, `msg ${i}`);
  botAssertEq(bot.log.length, 150);
  botAssertEq(bot.log[0].msg, "msg 199", "plus récent en tête");
  bot.log = save;
});
```

- [ ] **Step 4: Vérifier**

Recharger, démarrer bot, `Évaluer maintenant`, DevTools → `bot.log.slice(0,5)` doit contenir des entrées cohérentes. `botSelfTest()` → `25/25 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): journal de décisions (achats, sorties, refus dédupliqués)"
```

---

### Task 9: Apprentissage par famille (`botComputeLearning`)

**Files:**
- Modify: `terminal-tout-en-un.html` — ajouter `botComputeLearning(history, cfg, previous)` **pure**, l'appeler au début de chaque `runBot()` avant l'évaluation.

**Interfaces:**
- Consumes: `bot.history`, `bot.config`, ancien `bot.learn` (pour le lissage 10 %).
- Produces: `botComputeLearning(history, cfg, previous) → learn` ; assigné à `bot.learn` avant l'évaluation.

- [ ] **Step 1: Implémenter `botComputeLearning`**

Après `botLog` :

```js
function botClampMult(x)   { return clamp(x, 0.70, 1.30); }
function botClampQAdj(x)   { return clamp(x, 0, 15); }
// Déplace `from` vers `target` d'au plus 10 % de l'écart (lissage).
function botSmooth(from, target) { return from + (target - from) * 0.10; }

function botComputeLearning(history, cfg, previous = {}) {
  if (!cfg.learnEnabled) {
    // Recopie les stats mais tout est neutre
    const out = {};
    for (const k of Object.keys(previous)) out[k] = { ...previous[k], stopMult: 1, rrMult: 1, qualityAdj: 0 };
    return out;
  }
  const byFam = {};
  for (const h of history) {
    if (!h.family) continue;
    (byFam[h.family] = byFam[h.family] || []).push(h);
  }
  const out = { ...previous };
  for (const [family, trades] of Object.entries(byFam)) {
    const n = trades.length;
    const wins   = trades.filter(t => t.pnl > 0);
    const losses = trades.filter(t => t.pnl <= 0);
    const stops  = trades.filter(t => t.reason === "stop-loss");
    const winRate  = n > 0 ? wins.length / n : 0;
    const stopRate = n > 0 ? stops.length / n : 0;
    const avgWin   = wins.length   ? wins.reduce((s, t) => s + t.pnl, 0) / wins.length     : 0;
    const avgLoss  = losses.length ? Math.abs(losses.reduce((s, t) => s + t.pnl, 0) / losses.length) : 0;
    const avgPnl   = n > 0 ? trades.reduce((s, t) => s + t.pnl, 0) / n : 0;
    const prev = previous[family] || { stopMult: 1, rrMult: 1, qualityAdj: 0 };
    let stopMult = prev.stopMult, rrMult = prev.rrMult, qualityAdj = prev.qualityAdj;
    if (n >= cfg.learnMinTrades) {
      // Stop
      if      (stopRate > 0.45) stopMult = botSmooth(stopMult, 1.30);
      else if (stopRate < 0.15) stopMult = botSmooth(stopMult, 0.70);
      else                      stopMult = botSmooth(stopMult, 1.00);
      // RR
      if      (winRate > 0.60 && avgWin < avgLoss)         rrMult = botSmooth(rrMult, 1.30);
      else if (winRate < 0.40 && avgWin > 2 * avgLoss)     rrMult = botSmooth(rrMult, 0.70);
      else                                                  rrMult = botSmooth(rrMult, 1.00);
      // Quality adjust
      if      (n >= 15 && avgPnl < 0) qualityAdj = botClampQAdj(qualityAdj + 5);
      else if (n >= 15 && avgPnl > 0) qualityAdj = botClampQAdj(qualityAdj - 5);
    }
    out[family] = {
      stopMult: botClampMult(stopMult),
      rrMult:   botClampMult(rrMult),
      qualityAdj,
      n, winRate, stopRate, avgWin, avgLoss,
      updatedAt: new Date().toISOString(),
    };
  }
  // Familles sans trades : neutralisées / conservées telles quelles
  return out;
}
```

- [ ] **Step 2: Brancher dans `runBot` (avant le rafraîchissement)**

Juste après `const openMarkets = botOpenMarkets();` :
```js
    bot.learn = botComputeLearning(bot.history, cfg, bot.learn);
```

Adapter aussi le filtre candidats pour respecter `qualityAdj` :
```js
    .filter(x => x.e && x.e.ind && x.e.ind.price > 0 && !held.has(x.t) && x.e.score != null && (() => {
      const qadj = (bot.learn[`${botMarketOf(x.t)}:${botVolBucket((x.e.ind && x.e.ind.vol) || 30)}`] || {}).qualityAdj || 0;
      return computeGlobalScore(x.e) >= cfg.qualityMin + qadj;
    })() && openMarkets.has(botMarketOf(x.t)))
```

- [ ] **Step 3: Ajouter les tests**

```js
function botMkTrade(family, pnl, reason) {
  return { ticker: "X", family, entryDate: "2026-01-01", exitDate: "2026-01-10",
    entryPrice: 100, exitPrice: 100 + pnl / 10, qty: 10, pnl, pnlPct: pnl / 10, reason };
}

botTest("botComputeLearning: neutre sous 10 trades", () => {
  const h = Array.from({ length: 9 }, () => botMkTrade("US:moyenne", -10, "stop-loss"));
  const l = botComputeLearning(h, BOT_V2_DEFAULT_CONFIG, {});
  botAssertEq(l["US:moyenne"].stopMult, 1);
  botAssertEq(l["US:moyenne"].rrMult, 1);
  botAssertEq(l["US:moyenne"].qualityAdj, 0);
});

botTest("botComputeLearning: > 45 % de stops élargit le stopMult", () => {
  const h = [];
  for (let i = 0; i < 6; i++) h.push(botMkTrade("US:forte", -5, "stop-loss"));
  for (let i = 0; i < 4; i++) h.push(botMkTrade("US:forte",  8, "prise de bénéfice"));
  const l = botComputeLearning(h, BOT_V2_DEFAULT_CONFIG, {});
  botAssert(l["US:forte"].stopMult > 1, "stopMult devrait monter");
  botAssert(l["US:forte"].stopMult <= 1.30, "borné à 1.30");
});

botTest("botComputeLearning: lissage ≤ 10 %", () => {
  const h = Array.from({ length: 20 }, () => botMkTrade("US:forte", -5, "stop-loss"));
  const prev = { "US:forte": { stopMult: 1.0, rrMult: 1.0, qualityAdj: 0 } };
  const l = botComputeLearning(h, BOT_V2_DEFAULT_CONFIG, prev);
  botAssert(l["US:forte"].stopMult <= 1.0 + 0.10 * 0.30 + 1e-9, "≤ 3 % de mouvement par passe");
});

botTest("botComputeLearning: qualityAdj borné [0;15]", () => {
  const h = Array.from({ length: 20 }, () => botMkTrade("EU:moyenne", -10, "réévaluation"));
  let prev = { "EU:moyenne": { stopMult: 1, rrMult: 1, qualityAdj: 12 } };
  for (let i = 0; i < 5; i++) prev = botComputeLearning(h, BOT_V2_DEFAULT_CONFIG, prev);
  botAssert(prev["EU:moyenne"].qualityAdj <= 15);
});

botTest("botComputeLearning: learnEnabled=false → neutre", () => {
  const h = Array.from({ length: 20 }, () => botMkTrade("US:forte", -5, "stop-loss"));
  const cfg = { ...BOT_V2_DEFAULT_CONFIG, learnEnabled: false };
  const l = botComputeLearning(h, cfg, { "US:forte": { stopMult: 1.2, rrMult: 0.8, qualityAdj: 5 } });
  botAssertEq(l["US:forte"].stopMult, 1);
  botAssertEq(l["US:forte"].rrMult, 1);
  botAssertEq(l["US:forte"].qualityAdj, 0);
});
```

- [ ] **Step 4: Vérifier**

`botSelfTest()` → `30/30 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): apprentissage par famille (stopMult, rrMult, qualityAdj lissés)"
```

---

### Task 10: Statistiques + courbe de capital (`botStats`, `bot.equity`)

**Files:**
- Modify: `terminal-tout-en-un.html` — ajouter `botStats(history, equity)` **pure**, mise à jour de `bot.equity` en fin de `runBot()` (1 point/jour).

**Interfaces:**
- Consumes: `bot.history`, `bot.equity`, `botPortfolioValue`.
- Produces:
  - `botStats(history, equity) → { totalPct, drawdownPct, winRate, profitFactor, avgWin, avgLoss, expectancy, byMarket, byFamily, byReason, n }`
  - `botRecordEquity()` (mise à jour du dernier point du jour).

- [ ] **Step 1: Implémenter `botStats` et `botRecordEquity`**

```js
function botRecordEquity() {
  const today = new Date().toISOString().slice(0, 10);
  const value = botPortfolioValue();
  const last = bot.equity[bot.equity.length - 1];
  if (last && last.date === today) last.value = value;
  else bot.equity.push({ date: today, value });
  if (bot.equity.length > 400) bot.equity = bot.equity.slice(-400);
}

function botStats(history, equity) {
  const n = history.length;
  const wins   = history.filter(h => h.pnl > 0);
  const losses = history.filter(h => h.pnl <= 0);
  const sumW = wins.reduce((s, h) => s + h.pnl, 0);
  const sumL = Math.abs(losses.reduce((s, h) => s + h.pnl, 0));
  const winRate      = n ? wins.length / n : 0;
  const profitFactor = sumL > 0 ? sumW / sumL : (sumW > 0 ? Infinity : 0);
  const avgWin       = wins.length   ? sumW / wins.length : 0;
  const avgLoss      = losses.length ? sumL / losses.length : 0;
  const expectancy   = n ? (sumW - sumL) / n : 0;
  // Total et drawdown depuis equity
  const first = equity[0], last = equity[equity.length - 1];
  const totalPct = (first && last && first.value > 0) ? (last.value / first.value - 1) * 100 : 0;
  let peak = -Infinity, drawdown = 0;
  for (const p of equity) { if (p.value > peak) peak = p.value; if (peak > 0) drawdown = Math.max(drawdown, (peak - p.value) / peak); }
  const drawdownPct = drawdown * 100;
  const group = (keyOf) => {
    const g = {};
    for (const h of history) { const k = keyOf(h); if (!k) continue; (g[k] = g[k] || { n: 0, pnl: 0 }); g[k].n++; g[k].pnl += h.pnl; }
    return g;
  };
  return {
    n, totalPct, drawdownPct, winRate, profitFactor, avgWin, avgLoss, expectancy,
    byMarket: group(h => h.family ? h.family.split(":")[0] : null),
    byFamily: group(h => h.family),
    byReason: group(h => h.reason),
  };
}
```

- [ ] **Step 2: Appeler `botRecordEquity` en fin de `runBot`**

Juste avant `saveBot()` : `botRecordEquity();`.
Aussi dans `botStart()` (initialise le 1er point).

- [ ] **Step 3: Ajouter tests**

```js
botTest("botStats: drawdown max", () => {
  const eq = [{date:"d1",value:100},{date:"d2",value:120},{date:"d3",value:90},{date:"d4",value:110}];
  const s = botStats([], eq);
  botAssertClose(s.drawdownPct, (120-90)/120*100, 1e-6);
});

botTest("botStats: profit factor", () => {
  const h = [botMkTrade("US:moyenne", 30, "prise de bénéfice"), botMkTrade("US:moyenne", -10, "stop-loss")];
  const s = botStats(h, []);
  botAssertClose(s.profitFactor, 3, 1e-6);
});

botTest("botStats: groupements par marché/famille/raison", () => {
  const h = [
    botMkTrade("US:moyenne", 10, "prise de bénéfice"),
    botMkTrade("EU:faible",  -5, "stop-loss"),
    botMkTrade("US:moyenne",  2, "réévaluation"),
  ];
  const s = botStats(h, []);
  botAssertEq(s.byMarket.US.n, 2);
  botAssertEq(s.byFamily["EU:faible"].n, 1);
  botAssertEq(s.byReason["stop-loss"].n, 1);
});
```

- [ ] **Step 4: Vérifier**

`botSelfTest()` → `33/33 passed`.

- [ ] **Step 5: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): statistiques (drawdown, profit factor, groupements) et courbe de capital"
```

---

### Task 11: Refonte de l'UI F8 (bandeau session, sections, journal, stats, familles, réglages)

**Files:**
- Modify: `terminal-tout-en-un.html` — remplacer `renderBot()` (~ligne 4236), adapter le markup `#panel-bot` (~ligne 1057) pour ajouter les nouveaux conteneurs, adapter le handler `panel-bot` click/change.

**Interfaces:**
- Consumes: `bot`, `botStats`, `botOpenMarkets`, `botNextOpen`.
- Produces: rendu complet des sections A.2 (session), 2, 3, 4, 5, 6, 7, 8 du spec §G.

- [ ] **Step 1: Ajouter les conteneurs dans `#panel-bot`**

Chercher (~ligne 1055) le panneau bot et remplacer le bloc interne par :

```html
      <div id="bot-session" class="bot-session"></div>
      <span class="market-actions" id="bot-actions"></span>
      <details open><summary>Résumé</summary><div id="bot-summary" class="bot-summary"></div></details>
      <details open><summary>Positions ouvertes</summary><div id="bot-positions"></div></details>
      <details><summary>Statistiques</summary><div id="bot-stats"></div><div id="bot-equity"></div></details>
      <details><summary>Familles apprises</summary><div id="bot-learn"></div></details>
      <details><summary>Journal</summary><div id="bot-log"></div></details>
      <details><summary>Historique des trades</summary><div id="bot-history"></div></details>
      <details><summary>Réglages</summary><div id="bot-config" class="filter-panel"></div></details>
```

- [ ] **Step 2: Réécrire `renderBot`**

Remplacer entièrement `renderBot()` par une version qui :
- Écrit dans `#bot-session` : drapeaux et statut (`Europe ouverte` / `US ouvre dans 1 h 12`), + toggle boucle auto (`bot.config.autoLoop`), + heure prochaine évaluation.
- Rend le résumé avec exposition par marché (barres CSS simples avec `<div style="width: X%">`).
- Rend les positions : ticker, famille, entrée, cours, +/−, **stop effectif** (indiquer « suiveur » si `stopLevel > entryPrice*(1-stopPct/100)`), cible, partiel pris, jours restants (`horizonDays - (today - entryDate)`), bouton Vendre.
- Rend les stats : cartes compactes + petite courbe SVG (approche identique aux graphiques existants — chercher un exemple dans le fichier).
- Rend `bot.learn` : par famille, n, taux de réussite, part de stops, correctifs + justification en clair (« stop élargi à 1.15× — 52 % de stops »).
- Rend `bot.log` (25 dernières entrées).
- Historique : + colonnes famille et raison.
- Config : cases numériques via `BOT_CONFIG_FIELDS` + case cochable `autoLoop` + case cochable `learnEnabled`.

Le code complet est long : suivre le patron de la v1 (`impact-grid`, `data-table`, `table-wrap`). Utiliser `esc()` partout.

- [ ] **Step 3: Adapter le handler `change` pour les checkbox**

```js
document.getElementById("panel-bot").addEventListener("change", e => {
  const inp = e.target.closest(".js-bot-cfg");
  if (!inp) return;
  const key = inp.dataset.key;
  const val = inp.type === "checkbox" ? inp.checked : Number(inp.value);
  if (inp.type !== "checkbox" && !isFinite(val)) return;
  bot.config[key] = val;
  saveBot();
  if (key === "autoLoop" || key === "loopMinutes") botScheduleNext();
  renderBot();
});
```

- [ ] **Step 4: Vérifier manuellement**

Ouvrir `http://localhost:8750/terminal-tout-en-un.html`, F8 : bandeau session lisible, sections repliables, boucle on/off fonctionne. Démarrer, `Évaluer maintenant`, vérifier positions/journal/historique/stats/familles.

Tester en mobile (F12 → responsive). Non-régression : passer sur F1–F7 rapidement.

- [ ] **Step 5: `botSelfTest()` final**

`botSelfTest()` → tout passer (`33/33`).

- [ ] **Step 6: Tests serveur**

```bash
py -m unittest discover tests
```
Attendu : OK.

- [ ] **Step 7: Commit**

```bash
git add terminal-tout-en-un.html
git commit -m "feat(bot-v2): UI F8 refondue (session, stats, familles, journal, réglages)"
```

---

## Auto-revue

**Couverture du spec :**

- §A.1 `botMarketOf` → Task 1 ✓
- §A.2 `botOpenMarkets` + DST → Task 1 ✓
- §A.3 sortie en direct si ouvert, rejeu sinon → Task 3 + raffiné Task 5 ✓
- §A.4 boucle intraday, visibilité, réentrance → Task 4 + garde `botRunning` Task 3 ✓
- §B `botProfile` (tous les réglages) → Task 2 ✓
- §C apprentissage par famille → Task 9 ✓
- §D.1 stop suiveur → Task 5 ✓
- §D.2 sortie partielle → Task 6 ✓
- §D.3 plafonds position + marché → Task 2 (dans `botProfile`) ✓
- §D.4 frais/slippage → Task 7 ✓
- §D.5 horizon → **partiel** — l'horizon est calculé (Task 2) mais **la sortie horizon** manque explicitement dans le moteur. À ajouter en Task 5 ou 6.

**Correction inline (§D.5 sortie horizon) :** dans `runBot` boucle « b) sorties », **avant** le rejeu, ajouter :
```js
  const ageDays = (Date.now() - new Date(pos.entryDate).getTime()) / (1000 * 3600 * 24);
  let exit = (ageDays > pos.horizonDays) ? { price: (e && e.ind && e.ind.price > 0) ? e.ind.price : pos.entryPrice, date: today, reason: "horizon" } : null;
```
Ajouter à la Task 5 (ou en Task 6). Un test correspondant :
```js
botTest("horizon: sortie après dépassement", () => {
  // testé via une position artificielle et vérification que l'age > horizonDays entraîne exit
  const old = new Date(Date.now() - 100 * 86400000).toISOString().slice(0,10);
  botAssert(((Date.now() - new Date(old).getTime()) / 86400000) > 60);
});
```

**J'ajoute donc ce chapeau à la Task 5 :**

- **Step 0 (Task 5)** : Insérer la sortie « horizon » en tête de la boucle « b) sorties » — code ci-dessus — et un test dans `botSelfTest()`.

- §E journal (kinds, 150 entrées, dédup refus) → Task 8 ✓
- §F stats + equity + affichage → Task 10 + Task 11 ✓
- §G UI refondue → Task 11 ✓
- §H état persisté + migration → Task 3 ✓
- §I gestion des erreurs / cas limites → couverts par les défauts existants et le try/catch de `runBot`. La ligne « Reset vide learn/log/equity » est en Task 3.
- §J tests `botSelfTest()` → tous ajoutés au fil des tasks.
- §K découpage phases 1/2/3 → tasks 1-4 / 5-8 / 9-11 ✓

**Placeholders :** aucun (revérifié). Chaque step contient du code exécutable.

**Cohérence des noms :** `botMarketOf`, `botOpenMarkets`, `botNextOpen`, `botProfile`, `botApplyTick`, `botMigrateV1`, `botLog`, `botComputeLearning`, `botStats`, `botRecordEquity`, `botScheduleNext`, `botSelfTest` : utilisés de façon identique entre tasks. Champs de `bot` : `positions`, `history`, `log`, `equity`, `learn`, `config` — cohérents dans toutes les tasks.

---

## Handoff d'exécution

**Plan complet enregistré dans `docs/superpowers/plans/2026-07-20-bot-v2.md`. Deux options d'exécution :**

**1. Sous-agents (recommandé)** — je dispatche un sous-agent frais par task avec revue entre chaque, itération rapide.

**2. Exécution en ligne** — j'exécute les tasks dans cette session avec checkpoints pour revue.

**Quelle approche ?**
